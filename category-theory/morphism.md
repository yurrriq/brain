A **morphism** is a structure-preserving [[map]] (or **arrow**) between [[object|objects]] of the same type.

For example, $\mor{f}{X}{Y}$ is a **morphism** with [[domain]] (or **source**) $X$ and [[codomain]] (or **target**) $Y$.
