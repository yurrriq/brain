#+alias: monoids

A monoid $(M, \cdot)$ consists of:
- a set $M$
- an [[associative]] binary operation $\cdot$
- an identity element $1$

satisfying the following [[axioms]]:
 - *Associativity*: $(x \cdot y) \cdot z = x \cdot (y \cdot z)$
 - *Identity*: $1 \cdot x = x = x \cdot 1$
